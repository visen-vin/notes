# Arrow Functions

Arrow functions provide a concise syntax to write functions in JavaScript.

```javascript
const add = (a, b) => a + b;
```

## Features
- No own `this` binding
- Concise syntax
- Implicit return
